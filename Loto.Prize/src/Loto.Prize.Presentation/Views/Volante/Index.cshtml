@model IEnumerable<Loto.Prize.Presentation.Models.VolanteModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdJogo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumerosEscolhidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataVolante)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdJogo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumerosEscolhidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataVolante)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
